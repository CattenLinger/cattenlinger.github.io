---
title: 怎么安装 Remix OS 到本地
date: 2017-01-08 07:00:55
categories:
- ['笔记','应用实践']
tags:
- 'Remix OS'
- 'Android'
---
1. 首先你需要一个空的能启动的存储设备
2. 格式化为 ext4
3. 解压 Remix OS 的镜像
4. 拷 system.sfs initrd.img ramdisk.img 和 kernel 到盘的根目录
5. 创建一个 data 目录，权限改成 777 
6. 装 grub 引导：sudo grub-install --root-directory=到你安装目录的绝对路径 /dev/你的安装设备
7. 在安装目录的 boot/grub 创建 grub.cfg 然后写入以下内容

```
	set default=0
	set timeout=你自己喜欢等多少秒就写多少秒
	set gfxmode=1024x768
	terminal_output gfxterm
		
	menuentry 'Remix OS' --class android-x86{
	    search --file --no-floppy --set=root /kernel
	    linux /kernel root=/dev/ram0 androidboot.hardware=remix_cn_x86_64 androidboot.selinux=permissive quite SRC=/ DATA=/data
	    initrd /initrd.img
	}
```